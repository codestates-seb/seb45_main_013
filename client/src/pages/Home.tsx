import { styled } from 'styled-components';

const HomeContainer = styled.div``;

const Title = styled.div`
  width: 360px;
  height: 288px;
`;

const Home = () => {
  return (
    <HomeContainer>
      <Title />
    </HomeContainer>
  );
};

export default Home;
